machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - pip install --upgrade pip
    - pip install tox

test:
  override:
    - make clean
    - tox -e py27
    - tox -e py32
    - tox -e py33
    - tox -e py34
    - tox -e py35
    - tox -e pypy
