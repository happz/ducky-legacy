machine:
  python:
    version: 2.7.9

dependencies:
  pre:
    - pip install threading2
    - pip install enum34
    - pip install tabulate
    - pip install colorama
    - pip install pytty
    - pip install coverage
    - pip install lxml
    - pip install beautifulsoup4

test:
  override:
    - Q=@ CONIO_STDOUT_ECHO=no make tests
