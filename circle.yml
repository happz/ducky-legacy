machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - pip install --upgrade pip
    - pip install enum34
    - pip install tabulate
    - pip install colorama
    - pip install six
    - pip install coverage
    - pip install lxml
    - pip install beautifulsoup4
    - pip install nose-timer
    - pip install pycparser
    - pip install mock
    - pip install sphinx

test:
  override:
    - make clean
    - ./run-testsuite.sh 2.7.10
    - ./run-testsuite.sh 3.4.3
    - ./run-testsuite.sh 3.5.0
    - ./run-testsuite.sh pypy-2.5.0
#    - make clean
#    - ./run-coverage.sh 2.7.10
#    - ./run-coverage.sh 3.4.3
#    - ./run-coverage.sh --submit
