;
; RTC device defines
;

<%
  import ducky.devices.rtc
%>

.def RTC_IRQ:           ${X2(ducky.devices.rtc.DEFAULT_IRQ)}

.def RTC_DEFAULT_FREQ:  ${ducky.devices.rtc.DEFAULT_FREQ}

.def RTC_MMIO_ADDRESS:  ${X8(ducky.devices.rtc.DEFAULT_MMIO_ADDRESS)}

.def RTC_MMIO_FREQ:     ${X8(ducky.devices.rtc.RTCPorts.FREQUENCY)}
.def RTC_MMIO_SECOND:   ${X8(ducky.devices.rtc.RTCPorts.SECOND)}
.def RTC_MMIO_MINUTE:   ${X8(ducky.devices.rtc.RTCPorts.MINUTE)}
.def RTC_MMIO_HOUR:     ${X8(ducky.devices.rtc.RTCPorts.HOUR)}
.def RTC_MMIO_DAY:      ${X8(ducky.devices.rtc.RTCPorts.DAY)}
.def RTC_MMIO_MONTH:    ${X8(ducky.devices.rtc.RTCPorts.MONTH)}
.def RTC_MMIO_YEAR:     ${X8(ducky.devices.rtc.RTCPorts.YEAR)}
